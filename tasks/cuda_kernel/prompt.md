{description}

Operation: {operation_description}
Input length: {vector_length}
Kernel output path: {kernel_path}

Write a CUDA kernel that performs the described operation using the python_expression tool.
Persist the kernel to disk with write_kernel_file and then submit a JSON object containing the file_path and a short summary via submit_answer.

